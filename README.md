# Console-Devblog-To-Discourse

Gets the [Console Devblog](console.dev) RSS Feed and posts new entries to a discourse forum.
Look into `.env.example` for `.env` variables needed.

Otherwise it's just

```bash
npm i
npm run build
npm start
```
